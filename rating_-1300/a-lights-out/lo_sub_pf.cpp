#include <bits/stdc++.h>
using namespace std;

int main () {
  
  ios::sync_with_stdio(0);
  cin.tie(0);

  int x;
  
  for (int i = 0; i <= 2; ++i) {
    for (int j = 0; j <= 2; ++j) {
      cin >> x;
      cout << x;
    }
    cout << "\n";
  }

  return 0;
}
